# Bridge (runs on always-on LAN device)
PC_MAC_ADDRESS=AA:BB:CC:DD:EE:FF
BRIDGE_SECRET=change-me-to-a-random-secret

# Lambda (set via AWS console or CLI)
BRIDGE_URL=https://wake.yourdomain.com/wake
BRIDGE_SECRET=change-me-to-a-random-secret
