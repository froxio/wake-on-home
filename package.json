{
  "name": "wake-on-home",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "bridge",
    "lambda"
  ],
  "scripts": {
    "install:bridge": "npm install --workspace=bridge",
    "install:lambda": "npm install --workspace=lambda",
    "start:bridge": "npm run start --workspace=bridge",
    "deploy:lambda": "cd lambda && zip -r function.zip . && cd .."
  }
}
